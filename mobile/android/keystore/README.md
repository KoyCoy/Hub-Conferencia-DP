# Chave de assinatura

O arquivo `.jks` gerado para assinar o APK fica nesta pasta, mas nao e enviado ao GitHub.

Guarde essa chave com cuidado. Para atualizar o app por cima em celulares da equipe, as proximas versoes precisam usar:

- mesmo package name: `br.com.koycoy.hubrh`
- mesma chave de assinatura
- `versionCode` maior
